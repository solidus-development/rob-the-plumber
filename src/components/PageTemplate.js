import React from 'react';
import axios from 'axios';

// This is a template for a page component. 
// It is needed if you want the ability to create new pages from the WordPress dashboard.

class PageTemplate extends React.Component {
    render() {
      return (
        <div>
          <h1>Page Template</h1>
          <p>This is a page template. It still requires development if it is needed.</p>
        </div>
      );
    }
  }

  
export default PageTemplate;
